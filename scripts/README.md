# Scripts

- `validate_skill_package.py`: integrity/security/config validation.
- `claude_guard.py`: Claude Code PreToolUse guard for destructive commands and Live Execution activation.
- `generate_signal_id.py`: human-readable unique signal IDs.
- `render_signal_chart_reference.py`: offline reference renderer; integrate with project data/tests before production.

No script in this folder places exchange orders.

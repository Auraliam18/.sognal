# Runtime Skills

`runtime/skills/*.yaml` are machine-readable behavior contracts for the actual Python/LangGraph runtime engines.  
`.claude/skills/*/SKILL.md` teach Claude Fable 5 how to build, audit, test, and research those runtime engines.

They are deliberately separate: a Claude Code subagent is not a 24/7 market worker.

# حلقهٔ زندهٔ ۳۰ ثانیه‌ای روی لپ‌تاپ 🖥⚡

تا وقتی این سرویس روشن است، تأخیر سیستم از **~۳ دقیقه** (GitHub) به **~۳۰ ثانیه** می‌رسد.
اگر لپ‌تاپ خاموش شود هیچ چیزی نمی‌شکند — GitHub Actions خودش پشتیبان است و کف ۳ دقیقه برمی‌گردد.

## نصب — فقط یک بار (۵ دقیقه)

### مک
1. برنامهٔ **Terminal** را باز کن (Spotlight → بنویس Terminal)
2. این دو خط را یکی‌یکی کپی و Enter کن:
   ```
   git clone https://github.com/Auraliam18/.sognal.git ~/hamid-live
   open ~/hamid-live
   ```
   (اگر گفت `git` نیست، خودش پیشنهاد نصب می‌دهد — قبول کن و دوباره بزن)
3. در پوشه‌ای که باز شد، فایل `live.env.example` را کپی کن، نامش را بگذار `live.env`، بازش کن و توکن ربات تلگرام و chat id را جلوی مساوی‌ها بچسبان. ذخیره کن.
4. تمام.

### ویندوز
1. اول **Python** را نصب کن: python.org → Downloads → Install (تیک «Add to PATH» را حتماً بزن)
2. بعد **Git**: git-scm.com → Download → Install (همهٔ گزینه‌ها پیش‌فرض)
3. در منوی Start بنویس `cmd`، بازش کن و این را بزن:
   ```
   git clone https://github.com/Auraliam18/.sognal.git %USERPROFILE%\hamid-live
   ```
4. پوشهٔ `hamid-live` را در home خودت باز کن، `live.env.example` را کپی و به `live.env` تغییر نام بده، توکن‌ها را داخلش بگذار.

## اجرا — هر روز

| سیستم | کار |
|---|---|
| مک | دوبار کلیک روی **`live.command`** |
| ویندوز | دوبار کلیک روی **`live.bat`** |

پنجره‌ای باز می‌شود و هر ۳۰ ثانیه یک تیک می‌زند. **پنجره باز بماند = سرویس زنده.**

## چه کار می‌کند؟

- **هر ۳۰ ثانیه**: پایش دفتر انتظار پامپ (لحظهٔ حجم خوردن → سیگنال 🧠 فوری به تلگرام) + کشف شعله‌گیری ۱۵ دقیقه‌ای
- **هر ۵ دقیقه**: رادار کامل پامپ — کشف خوشهٔ تازه و ثبت دنباله‌روهای ساکت در دفتر
- **هر ۲ دقیقه**: همگام‌سازی با GitHub تا سرویس لپ‌تاپ و Actions همدیگر را ببینند و سیگنال تکراری نفرستند
- **آلارم خودکار**: تیکِ کند یا قطع منبع داده → پیام 🚨 به تلگرام (حداکثر یکی در ساعت)
- ضربان سرویس در `signals/live-heartbeat.json` ثبت می‌شود — اگر بایستد، از روی پنل معلوم است

## توقف

پنجره را ببند یا `Ctrl+C`. سیستم به حالت GitHub-فقط (کف ~۳ دقیقه) برمی‌گردد — هیچ سیگنالی گم نمی‌شود، فقط کندتر می‌شود.

## جلوگیری از خواب لپ‌تاپ

- **مک**: System Settings → Battery → جلوی خواب هنگام شارژر را بگیر؛ یا در Terminal یک بار: `caffeinate -i` در پنجرهٔ دوم
- **ویندوز**: Settings → System → Power → Sleep = Never (هنگام اتصال به برق)
